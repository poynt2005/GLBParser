{
  "name": "node",
  "version": "1.0.0",
  "description": "",
  "main": "path.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "install": "node-gyp rebuild",
    "build-dll": "mingw32-make dll",
    "build-native": "npm run build-dll && powershell -File .\\build.ps1"
  },
  "author": "",
  "license": "ISC",
  "gypfile": true,
  "devDependencies": {
    "node-addon-api": "^6.0.0"
  }
}